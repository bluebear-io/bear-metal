# DEN-2376 — Change bear-metal startup log message

## Goal

Replace the bear-metal manager startup log message from `hello! i'm up!` to `🐻 Bear Metal is awake and hungry for tickets — let's ship some code!`.

## Files touched

- `bear-metal/src/manager/index.ts`: update the `logger.info` string inside the `app.listen` callback.

## Verification

- `grep` confirms the new string appears and the old one is gone.
- No behavioral change beyond the log string; existing tests remain valid.
