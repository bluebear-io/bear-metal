# DEN-2376 — Change Bear Metal startup log message

## Goal
Change the manager's startup log message from `"hello! i'm up!"` to
`"🐻 Bear Metal is awake and hungry for tickets — let's ship some code!"`.

## Files touched
- `bear-metal/src/manager/index.ts` — single `logger.info` string in the
  health-server `listen` callback.

## Verification
- `grep` confirms the new string is in place and the old one is gone.
- No behavioural change beyond the log text; existing tests remain valid.
