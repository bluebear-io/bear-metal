# DEN-2376 — Change bear-metal startup log message

## Goal

Replace the bear-metal manager startup log message from `"hello! i'm up!"` to `"🐻 Bear Metal is awake and hungry for tickets — let's ship some code!"`.

## Files touched

- `bear-metal/src/manager/index.ts`: update the second `logger.info` call inside `app.listen(...)` callback to log the new message.

## Verification

- `grep` confirms the old string is gone and the new string is present.
- `pnpm tsc --noEmit` (or equivalent build) passes.
