# DEN-2383 — Change Bear Metal startup log message

## Goal
Change the manager startup log message from
`"🐻 Bear Metal is awake and hungry for tickets — let's ship some code!"`
to `"hello! i'm up!"`.

## Files touched
- `bear-metal/src/manager/index.ts` — replace the second `logger.info` string inside the `app.listen` callback.

## Verification
- `grep` confirms the old string is gone and the new string is present.
- Manager boot logs will emit `"hello! i'm up!"` after the health server listens.
