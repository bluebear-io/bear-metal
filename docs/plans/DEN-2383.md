# DEN-2383 — make me a pizza

## Spec

Per Linear comment from reporter:
> when bear metal manager starts, it should print "make me a pizza" instead of "🐻 Bear Metal is awake and hungry for tickets — let's ship some code!"

## Files touched

- `bear-metal/src/manager/index.ts` — change the second `logger.info` inside `app.listen(...)` startup callback from the current bear/pizza-less startup message to `"make me a pizza"`.

## Verification

- Manual: start the manager; first non-"health server listening" log line on startup should be `make me a pizza`.
- No test added: the message is a static log string with no business logic.
