# DEN-2383: change bear metal startup log message

## Goal
Change the manager startup log message from
`"🐻 Bear Metal is awake and hungry for tickets — let's ship some code!"` to
`"hello! i'm up!"`.

## Files touched
- `bear-metal/src/manager/index.ts`: update the `logger.info` string inside `app.listen(...)`.

## Verification
- `grep` confirms no remaining occurrences of the old message in `src/`.
- The new string appears at the same logger.info call site.
