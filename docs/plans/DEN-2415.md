# DEN-2415 — Change bear metal startup log message to 'hi'

## Intent
Replace the manager startup log message with the literal string `hi` as specified in the ticket title.

## Files touched
- `src/manager/index.ts` — replace the second `logger.info` call's message string with `"hi"`.

## Verification
- `git diff` shows only the message string changed.
- `tsc` compiles cleanly.
