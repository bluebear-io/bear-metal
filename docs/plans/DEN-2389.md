# DEN-2389 — Change Bear Metal startup log message to "make me a pizza you beautiful BEAR!"

## Goal
Update the manager startup log line from
`"🐻 Bear Metal is awake and hungry for tickets — let's ship some code!"` to
`"make me a pizza you beautiful BEAR!"`.

## Files Touched
- `bear-metal/src/manager/index.ts` — replace the string literal passed to
  `logger.info` after the health server starts listening.

## Verification
- `grep` confirms the new message replaces the old one in
  `src/manager/index.ts`.
- No behavior change beyond the log message text; no tests are applicable
  (string literal only).
