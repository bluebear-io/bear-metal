# DEN-2373 — Change startup log message to "hello! i'm up!"

## Goal
Update the manager startup log line so the message reads exactly `hello! i'm up!`.

## Scope
- Replace the existing manager startup log message in `src/manager/index.ts` (the `logger.info` call emitted right after the health server begins listening) with the string `hello! i'm up!`.
- Drop the previous bear-themed flavor text and emoji from this line.
- Keep the structured-log context (`port`, `pid`) intact.

## Out of scope
- The "config loaded" log line.
- The shutdown / hibernation log lines.
- Any other logger output, log levels, or formatting.

## Files touched
- `src/manager/index.ts` — single string change on the startup log line.
- `docs/plans/DEN-2373.md` — this plan.

## Verification
- `grep` for the new message and the old message to confirm exactly one replacement.
- `npm run typecheck` (or equivalent) passes locally.
- Manual review of the diff: only the message string changes; structured fields unchanged.
