# DEN-2373 — Change bear-metal startup log message to "hello! i'm up!"

## Goal
Replace the bear-metal manager startup log message with the literal string `hello! i'm up!`.

## Scope
- `bear-metal/src/manager/index.ts`: change the second `logger.info` call inside `app.listen(...)` (currently the "🐻 Bear Metal is awake and hungry for tickets…" string) to log `"hello! i'm up!"`.

## Out of scope
- Shutdown log messages.
- The pre-existing `"health server listening"` line.
- Any other logging or behavior changes.

## Verification
- Inspect the diff: only the message string changes; structured fields (`port`, `pid`) unchanged.
- `npm run build` (or equivalent typecheck) in `bear-metal/` succeeds.
