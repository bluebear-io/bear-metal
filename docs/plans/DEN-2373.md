# DEN-2373 — Change bear-metal startup log message

## Ticket
Change the startup log message in bear metal to `hello! i'm up!`.

## Scope
- Update the manager startup log message emitted when the health server begins listening.

## Files touched
- `src/manager/index.ts` — replace the `🐻 Bear Metal is awake and hungry for tickets — let's ship some code!` message with `hello! i'm up!`.

## Out of scope
- Shutdown log messages.
- Other emoji/branding messages elsewhere in the codebase.

## Verification
- `grep` confirms the new string is the only startup log payload at the `app.listen` callback.
- `npm run typecheck` / build remains green (string-only change).
