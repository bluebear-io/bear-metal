# bear-metal

Background worker for Linear-driven coding tasks.

Required runtime environment:

- `GITHUB_TOKEN`
- `LINEAR_API_KEY`
- Pi model credentials supported by `@earendil-works/pi-coding-agent`
